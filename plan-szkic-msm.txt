Project: mini social media

Co nowego poznam: Flusk oraz struktura programu(tylko już samodzielnie a nie AI XDD)

Co musi robić kod:
1. Strona wstępna(np. "Qbi - świat możliwości (rejestracja)")
2. Portal rejestracji - użytkownik wprowadza dane i wysyłane są w jsonie
3. Serwer tworzy użytkownika, + dodajemy unikatowy ID. Nie dawaj
   możliwości tworzenia podobnej nazwy i ID. Pilnuj bezpieczeństwa
4. Przejście do strony głównej. Tutaj wyświetlamy randomowe posty(no, nie umiem w algorytmy social media)
5. Tworzenie postu. PILNUJ BEZPIECZEŃSTWO!!!


    My tree:

    main.py - główny skrypt; uruchamia stronę oraz pliki.
        routes.py - akcje strony #NIE ROOTS
        db_utils - polecenia do socialbase
    socialbase.db - baza danych
    templates/
        AI generated HTML files
    static/
        pliki css i js, czyli szablony, które będzie wykorzystywać strona(np. formularz rejestracji)


Harmonogram(może ulec zmianom):
0.)Create a repo
I.) "db_utils":
        registration
        sign up
        post creating
        post recall(take user info and post info together)
        post algorythm(random)
        user recall
        user class - roots
        post class - zmiana na roots
        testy
II.) "routes"
        old plan:
        understanding how to do routing
        defining all actions
        writing code(write later)
        new plan:
        so, I found a good video on youtube about doing project on flask. I'm just going to repeat that.
        User class plan:
            1. update data
            2. pack in dict and json for sending
            3. remember about private and public data.
            4.
III.) Generacja AI
WAŻNE: wskaż ajajce, że musi być w JavaScripcie Frontend. Bo robię system pod wysyłanie JSONów, a nie form itd.
IV.) Main, czyli układanie wszystko w jeden kawał

for test branch(has to be deleted later)

Ideas for further functions:
1.) Daily login table like GitHub. Because it encourages me to go on this website.
2.) To do the theme of site as WritingHub or something like that. Writers writes their posts, publics their manuscripts. Also they get that daily activities
3.) Make theme as achievements. You could create your daily marks for different acticvities, to create groups or themes like on Reddit or smth. 
4.) To make manual algorithm option. Like you choosing what to read or watch intentionally: you get some hashtags, themes and channels, so you form your fyp. Also there is an option for no algorithm or "light" algorithm(like lesser part of fyp is gonna be algorytmically chosen. Other part is gonna come from random reccomendations.
